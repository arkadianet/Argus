# Copy to .env locally. Never commit real keystore paths or passwords.

# Android release signing (required for `flutter build apk` / --release).
# Debug builds do not need these. Copy to .env locally; never commit real values.
ARGUS_KEYSTORE=
ARGUS_KEYSTORE_PASSWORD=
ARGUS_KEY_ALIAS=
ARGUS_KEY_PASSWORD=
