<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- HTTPS everywhere. User-added nodes must serve TLS; a bare host in
         Settings is upgraded to https:// by NetworkController. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
